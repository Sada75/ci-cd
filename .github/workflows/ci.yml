name : code-check

on:
  push:
    branches:
      -master

jobs:
  run-check:
    runs-on: ubuntu-latest

    steps:
      - name : checkout repository
        uses : actions/checkout@v4
      
      - name : run check script
        run : ../checks/check.sh